---
// Componente ItemMenu.astro
const { img, name, section} = Astro.props;
---
<a href={section} class=" flex flex-row gap-1 text-red font-semibold text-[#ffff] text-pretty">
   
    <span>{name}</span>
</a>